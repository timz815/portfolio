**Product Design Portfolio**

written in html, css, and vanilla javascript

Media lives in `media/`, grouped by use:

- `icons/`: site icons and favicon
- `home/`: homepage media
- `about/`: About page media
- `calendar/`, `wrco/`, `translate/`: project media, including homepage card artwork

Use relative media paths in pages and scripts so assets work locally and when deployed.
